git status

git clone "link of repository"

git add --all

git add "Basic Git Commands.txt"

git restore --staged "Feature2.txt"

git commit -m "First Commit"

 git config --global user.email "you@example.com"
  git config --global user.name "Your Name"

git push

